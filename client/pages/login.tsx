import React from 'react';
import LoginForm from '../components/LoginForm';

export default () => {
  // let pageProps = {
  //   pageTitle: 'login'
  // };
  // return <LoginForm pageName="login" />;
  return <LoginForm />;
};
